# -*- Makefile -*-

CFLAGS   += ${EXT_CFLAGS}
CXXFLAGS += ${EXT_CXXFLAGS}
ASFLAGS  += ${EXT_ASFLAGS}
LDFLAGS  += ${EXT_LDFLAGS}
SOFLAGS  += ${EXT_SOFLAGS}
ARFLAGS  += ${EXT_ARFLAGS}

ifeq (${V},0)
    Q := @
    VECHO_CC  = ${Q}echo "  CC      " $@
    VECHO_CXX = ${Q}echo "  CXX     " $@
    VECHO_LD  = ${Q}echo "  LD      " $@
endif

#==============================================================================
